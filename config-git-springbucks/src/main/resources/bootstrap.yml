spring:
  application:
    name: springbucks-service
  cloud:
    # consul
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true
    # config
    config:
      # uri: http://localhost:8888
      discovery:
        enabled: true
        service-id: configserver